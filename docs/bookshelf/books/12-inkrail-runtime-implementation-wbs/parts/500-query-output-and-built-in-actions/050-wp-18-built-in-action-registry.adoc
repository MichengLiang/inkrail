[#wp-18-built-in-action-registry.work-package, owner=query-runtime, wp=WP-18, status=未开始]
== WP-18：Built-in Action Registry

目标:: 建立内置查询动作 registry。Registry 保存 action id、CLI path、query kind、parameter schema、SPARQL template、variables、sort contract 和 source workflow reference。

上游门槛:: xref:wp-16-raw-query-modes[WP-16, rel=blocks-on] 通过。

上下文闭包:: xref:{book-10}#query-template-contract[10 号 query template contract]、xref:{book-10}#heading-actions[heading actions]、xref:{book-10}#structure-actions[structure actions]、xref:{book-10}#xref-actions[xref actions]、xref:{book-10}#degree-and-neighborhood-actions[degree/neighborhood actions] 和 xref:{book-10}#audit-actions[audit actions]；xref:{book-08}#query-library[08 号 query library]、xref:{book-08}#heading-location-and-slices[heading location]、xref:{book-08}#structural-scope-queries[structural scope]、xref:{book-08}#xref-edge-queries[xref edge]、xref:{book-08}#xref-degree-queries[xref degree]、xref:{book-08}#xref-neighborhood-queries[xref neighborhood] 和 xref:{book-08}#quality-audit-queries[quality audit]；xref:{book-11}#built-in-action-flow[11 号 built-in action flow]。

文件边界::

[source,text]
----
packages/inkrail/src/core/query-actions.ts
packages/inkrail/src/services/built-in-actions.ts
packages/inkrail/src/**/*.{test,spec}.ts
----

实施任务:: 定义 source、outline、heading locate/raw、labels、structure、xrefs、degree、neighborhood、audit action definitions。每个 definition 保留 query source reference 和 exact variables/sort contract。实现 SPARQL rendering 和 `printQuery` result。

测试任务:: 用 fixture 或 snapshots 锁定 SPARQL templates、SELECT variables、ORDER BY、OPTIONAL、FILTER NOT EXISTS、GROUP BY、HAVING、property path、parameter interpolation 和 source reference。

验证命令::

[source,bash]
----
pnpm --filter inkrail test
pnpm --filter inkrail typecheck
----

提交规则:: 实现提交只包含本工作包文件边界内的源码、测试、文档、fixture 或配置修改。审核证据提交只包含本工作包 review package、状态台账和必要的计划书记录更新。文件边界外修改必须写入审核包的受审例外。

审核规则:: 审核者必须核对目标依据、文件边界、实际命令、命令结果、未处理对象去向、全局打回条件和解锁关系。缺少提交 hash、review package 或实际命令结果时，本工作包不得进入 `通过`。

验收标准:: Registry 是 built-in action 语义唯一来源。CLI command mapping 只消费 registry。`--print-query` 在 render 后停止。

打回条件:: SPARQL 凭记忆改写；registry 缺 source workflow reference；action 改变事实边界；parameter interpolation 可注入 arbitrary SPARQL structure。

解锁关系:: 本工作包通过后解锁 xref:wp-19-built-in-cli-command-mapping[WP-19, rel=unlocks]。
