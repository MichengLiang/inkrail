[#bookshelf-runtime-dispatch-flow.runtime-flow, owner=bookshelf-runtime]
== 书架 Runtime 调度流

书架 Runtime 调度流执行标准书架本地 package scripts。它实现 xref:../10-inkrail-cli-design/book.adoc#bookshelf-build-dispatch[10 号书书架本地构建调度, rel=implements-contract]。

[source,text]
----
bookshelf runtime input
  -> load project
  -> resolve registered bookshelf
  -> require standard profile
  -> validate package script
  -> validate tools/adoc-books.mjs
  -> run package script in bookshelf root
  -> forward stdout/stderr/status
  -> return dispatch result
----

支持的 actions 是 `build`、`check` 和 `clean`。Dispatch 使用登记的 `bookshelves.root` 作为 process cwd。

未知 bookshelf 返回 `BOOKSHELF_NOT_FOUND`。Non-standard profile 返回 `BOOKSHELF_PROFILE_UNSUPPORTED`。缺失 package script 返回 `BOOKSHELF_SCRIPT_NOT_FOUND`。缺失 `tools/adoc-books.mjs` 返回 `BOOKSHELF_RUNTIME_NOT_FOUND`。Non-zero script status 返回 `BOOKSHELF_BUILD_FAILED`。

Bookshelf runtime dispatch 不写 RDF projection state。Project mode query 不隐式执行 bookshelf local `build`、`check` 或 `clean`。
