[#xref-patterns]
== 引用模式

书内引用使用本书内 anchor：

[source,asciidoc]
----
xref:#decision-table[结构选择表]
----

跨书引用写向 AsciiDoc 源文件：

[source,asciidoc]
----
xref:../01-starter-book/book.adoc#starter-book-boundary[单书边界]
----

被跨书引用的位置应写显式 anchor。显式 anchor 是维护者可以稳定依赖的目标。
