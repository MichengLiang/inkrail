[#build-artifacts]
== 投影生成物

RDF12 TTL 是 `asciidoc-abundant-tree` 的 `rdf12` Turtle 输出。标准 profile 下，它位于：

[source,text]
----
build/rdf12/books/<book-id>.ttl
----

RDF12 TTL 是项目模式查询的输入。Oxigraph 加载该文件并执行 SPARQL。

RDF12 JSON-LD 是 `asciidoc-abundant-tree` 的 `rdf12-json-ld` 输出。标准 profile 下，它位于：

[source,text]
----
build/rdf12/books/<book-id>.jsonld
----

TTL 与 JSON-LD 的生成纪律来自 xref:../08-rdf12-heading-projection-query-workflow/book.adoc#build-and-projection[RDF 投影查询工作流的投影输入与生成物章节, rel=depends-on]。

HTML 是发布表面。标准 profile 下，它位于：

[source,text]
----
build/html/books/<book-id>/book.html
----

TTL、JSON-LD 和 HTML 都是生成物。它们可以删除并重新生成。它们不作为手写源文件维护。

HTML 构建不参与 `inkrail query` 的新鲜度判断。查询前刷新只要求 RDF12 TTL 代表当前 book entry source set。
