{
  "name": "inkrail-workspace",
  "version": "0.1.0",
  "private": true,
  "license": "PolyForm-Noncommercial-1.0.0",
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "test": "pnpm --dir docs/bookshelf test",
    "build": "pnpm --dir docs/bookshelf build",
    "versions:check": "node scripts/check-versions.mjs",
    "pack:check": "pnpm --filter inkrail pack:check",
    "check": "pnpm versions:check && pnpm test && pnpm build && pnpm pack:check"
  }
}
